# Base URL of the AdamHUB backend API (FastAPI, served from app/).
# Matches the backend's local dev convention — see the repo root .env.example.
# Vars prefixed EXPO_PUBLIC_ are inlined into the JS bundle at build time.
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
