## Summary

<!-- Provide a short bullet-point summary of the changes in this PR. -->

- ...

## Related issue

<!-- Link the issue this PR closes, if any. -->

Closes #<!-- issue number -->

## Test plan

Check what applies:

- [ ] Backend tests pass (`uv run pytest`)
- [ ] Web build + lint pass (`npm run build`, `npm run lint` in `web/`)
- [ ] App-SaaS typecheck + lint pass (`npm run typecheck`, `npm run lint` in `app-saas/`)
- [ ] Manually verified in browser or app

> Note: no CI-required checks are enforced yet, this is informational.
